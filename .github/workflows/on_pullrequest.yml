name: On Pull Request

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'docs/**'

jobs:
  call-build:
    uses: ./.github/workflows/build.yml
    with:
      uv_version: "0.10.9"
